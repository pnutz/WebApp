<%= form_for(@receipt_category) do |f| %>
  <% if @receipt_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@receipt_category.errors.count, "error") %> prohibited this receipt_category from being saved:</h2>

      <ul>
      <% @receipt_category.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

	 <div class="field">
    <%= f.label :category %><br>
    <%= f.text_field :category %>
  </div>
	
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
